<Window x:Class="WPFSoft.Views.EmployessView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WPFSoft.Views"
        xmlns:vm="clr-namespace:WPFSoft.ViewModels"
        mc:Ignorable="d"
        Title="EmployessView" Height="450" Width="800">
    <Window.DataContext>
        <vm:EmployeesViewModel/>
    </Window.DataContext>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <TextBox MinHeight="30" Text="{Binding Filter, UpdateSourceTrigger=PropertyChanged}"/>
        <ListView Grid.Row="1" ItemsSource="{Binding EmployeesView, UpdateSourceTrigger=PropertyChanged}" MouseDoubleClick="ListView_MouseDoubleClick">
            <ListView.View>
                <GridView>
                    <GridViewColumn DisplayMemberBinding="{Binding LastName}">
                        <GridViewColumnHeader Content="LastName"/>
                    </GridViewColumn>
                    <GridViewColumn DisplayMemberBinding="{Binding FirstName}">
                        <GridViewColumnHeader Content="FirstName"/>
                    </GridViewColumn>
                    <GridViewColumn DisplayMemberBinding="{Binding Age}">
                        <GridViewColumnHeader Content="Age"/>
                    </GridViewColumn>
                    <GridViewColumn DisplayMemberBinding="{Binding CityName}">
                        <GridViewColumnHeader Content="City"/>
                    </GridViewColumn>
                </GridView>
            </ListView.View>
        </ListView>

    </Grid>
</Window>
